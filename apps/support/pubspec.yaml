name: gestoria_support
description: Support HQ — kanceláře, licence, převtělení.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  easy_localization: ^3.0.7
  flutter_riverpod: ^2.6.1
  go_router: ^16.2.4
  gestoria_auth:
    path: ../../packages/gestoria_auth

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  shared_preferences: ^2.5.3

flutter:
  uses-material-design: true
  assets:
    - assets/translations/
