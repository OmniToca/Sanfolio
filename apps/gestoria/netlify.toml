# Flutter web SPA.

[build]
  command = "flutter build web --release --dart-define-from-file=../../config.json"
  publish = "build/web"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
