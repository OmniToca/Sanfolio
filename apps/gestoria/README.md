# Portal Gestoría (Flutter web)

```bash
cd apps/gestoria
flutter pub get
flutter run -d chrome --web-port=5555 --dart-define-from-file=../../config.json
```

Bez `config.json` uvidíš přihlášení a hlášku, že chybí klíče. Support app: [`apps/support`](../support/README.md).
